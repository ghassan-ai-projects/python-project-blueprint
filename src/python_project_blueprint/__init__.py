"""Minimal package that keeps the template importable and testable."""

from __future__ import annotations


def project_name() -> str:
    """Return the placeholder project name for the template package."""
    return "python-project-blueprint"
